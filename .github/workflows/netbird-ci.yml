name: Ubuntu VPS - Full Desktop + TurboVNC

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - uses: actions/checkout@v4

      # ============================================
      # 1. INSTALL NETBIRD
      # ============================================
      - name: Install NetBird
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ca-certificates curl gnupg
          curl -sSL https://pkgs.netbird.io/debian/public.key | sudo gpg --dearmor --output /usr/share/keyrings/netbird-archive-keyring.gpg
          echo 'deb [signed-by=/usr/share/keyrings/netbird-archive-keyring.gpg] https://pkgs.netbird.io/debian stable main' | sudo tee /etc/apt/sources.list.d/netbird.list
          sudo apt-get update -y
          sudo apt-get install -y netbird

      # ============================================
      # 2. CONNECT NETBIRD
      # ============================================
      - name: Connect NetBird
        env:
          NETBIRD_SETUP_KEY: ${{ secrets.NETBIRD_SETUP_KEY }}
        run: |
          sudo netbird up --setup-key $NETBIRD_SETUP_KEY --allow-server-ssh &
          sleep 5
          sudo netbird status

      # ============================================
      # 3. CONFIGURE NETWORKMANAGER
      # ============================================
      - name: Configure NetworkManager
        run: |
          sudo mkdir -p /etc/NetworkManager/conf.d
          sudo tee /etc/NetworkManager/conf.d/99-netbird-unmanaged.conf << 'EOF'
          [keyfile]
          unmanaged-devices=interface-name:wt0
          EOF
          sudo systemctl restart NetworkManager 2>/dev/null || true

      # ============================================
      # 4. INSTALL FULL UBUNTU DESKTOP + TURBOVNC
      # ============================================
      - name: Install Full Desktop + TurboVNC
        run: |
          sudo apt-get update -y
          
          # Install full Ubuntu desktop (GNOME)
          sudo apt-get install -y \
            ubuntu-desktop \
            xrdp \
            openssh-server \
            net-tools \
            iputils-ping \
            dnsutils \
            traceroute \
            curl \
            wget \
            iperf3 \
            tcpdump \
            nmap \
            mtr \
            whois \
            telnet \
            netcat-openbsd \
            socat \
            bridge-utils \
            ethtool \
            conntrack \
            iptables \
            nftables \
            openvpn \
            wireguard \
            rsync \
            screen \
            tmux \
            htop \
            ncdu \
            git \
            vim

      # ============================================
      # 5. INSTALL TURBOVNC
      # ============================================
      - name: Install TurboVNC
        run: |
          LATEST_URL=$(curl -s https://api.github.com/repos/TurboVNC/turbovnc/releases/latest | grep "browser_download_url.*amd64.deb" | cut -d '"' -f 4)
          if [ -z "$LATEST_URL" ]; then
            echo "❌ TurboVNC not found"
            exit 1
          fi
          echo "✅ Downloading: $LATEST_URL"
          wget -O /tmp/turbovnc_latest_amd64.deb "$LATEST_URL"
          sudo apt-get install -y /tmp/turbovnc_latest_amd64.deb
          export PATH="/opt/TurboVNC/bin:$PATH"
          echo 'export PATH="/opt/TurboVNC/bin:$PATH"' >> ~/.bashrc
          echo "✅ TurboVNC installed"

      # ============================================
      # 6. CONFIGURE SSH (root/root)
      # ============================================
      - name: Configure SSH
        run: |
          echo "root:root" | sudo chpasswd
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh

      # ============================================
      # 7. CONFIGURE RDP (root/root)
      # ============================================
      - name: Configure RDP
        run: |
          # Allow root login via RDP
          sudo sed -i 's/^.*allowed_users=.*$/allowed_users=anybody/' /etc/X11/Xwrapper.config
          
          # Create root .xsession
          echo "gnome-session" > /root/.xsession

          sudo tee /etc/xrdp/startwm.sh > /dev/null << 'EOF'
          #!/bin/sh
          . /etc/profile
          export HOME=/root
          export XDG_CURRENT_DESKTOP="GNOME"
          export XDG_MENU_PREFIX="gnome-"
          export GNOME_SHELL_SESSION_MODE="ubuntu"
          exec gnome-session
          EOF
          sudo chmod +x /etc/xrdp/startwm.sh
          sudo systemctl restart xrdp
          sudo systemctl enable xrdp

      # ============================================
      # 8. CONFIGURE TURBOVNC (root/root)
      # ============================================
      - name: Configure TurboVNC
        run: |
          export PATH="/opt/TurboVNC/bin:$PATH"
          
          # Setup root VNC directory
          mkdir -p /root/.vnc
          echo "root" | /opt/TurboVNC/bin/vncpasswd -f > /root/.vnc/passwd
          chmod 600 /root/.vnc/passwd

          # Create xstartup for root
          tee /root/.vnc/xstartup > /dev/null << 'EOF'
          #!/bin/sh
          export XDG_CURRENT_DESKTOP="GNOME"
          export XDG_MENU_PREFIX="gnome-"
          export GNOME_SHELL_SESSION_MODE="ubuntu"
          unset SESSION_MANAGER
          unset DBUS_SESSION_BUS_ADDRESS
          exec gnome-session
          EOF
          chmod +x /root/.vnc/xstartup

          # Start TurboVNC on display :1 (port 5901)
          /opt/TurboVNC/bin/vncserver :1 -geometry 1920x1080 -depth 24

          echo "✅ TurboVNC running on port 5901"

      # ============================================
      # 9. VERIFY SERVICES
      # ============================================
      - name: Verify Services
        run: |
          export PATH="/opt/TurboVNC/bin:$PATH"
          echo "=== SSH ==="
          sudo systemctl status ssh --no-pager | head -3
          echo ""
          echo "=== xRDP ==="
          sudo systemctl status xrdp --no-pager | head -3
          echo ""
          echo "=== TurboVNC ==="
          /opt/TurboVNC/bin/vncserver -list
          echo ""
          echo "=== NetBird ==="
          sudo netbird status

      # ============================================
      # 10. CONNECTION INFO
      # ============================================
      - name: Connection Info
        run: |
          IP=$(sudo netbird status | grep "NetBird IP" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          echo "=========================================="
          echo "✅ UBUNTU VPS - FULL DESKTOP + TURBOVNC"
          echo "=========================================="
          echo ""
          echo "📡 NetBird IP: $IP"
          echo ""
          echo "🔑 SSH (port 22):"
          echo "  ssh root@$IP"
          echo "  Password: root"
          echo ""
          echo "🖥️ xRDP (port 3389):"
          echo "  Address: $IP:3389"
          echo "  User: root"
          echo "  Password: root"
          echo "  → Full Ubuntu GNOME Desktop (ROOT)"
          echo ""
          echo "🖥️ TurboVNC (port 5901):"
          echo "  vncviewer $IP:5901"
          echo "  Password: root"
          echo "  → Full Ubuntu GNOME Desktop (ROOT)"
          echo ""
          echo "📦 Network Tools: net-tools, ping, dig, traceroute"
          echo "  curl, wget, iperf3, tcpdump, nmap, mtr"
          echo "  telnet, netcat, openvpn, wireguard"
          echo "  screen, tmux, htop, git, vim"
          echo ""
          echo "🔧 NetworkManager: RUNNING - wt0 UNMANAGED"
          echo "👑 ALL SERVICES USE root/root"
          echo "⏱️ Server runs for 6 HOURS"
          echo "=========================================="

      # ============================================
      # 11. KEEP ALIVE
      # ============================================
      - name: Keep Alive (6 hours)
        run: |
          echo "=========================================="
          echo "🚀 SERVER RUNNING - FULL DESKTOP"
          echo "=========================================="
          echo ""
          echo "✅ NetBird: CONNECTED"
          echo "✅ SSH: ACTIVE (root/root)"
          echo "✅ xRDP: ACTIVE (root/root on 3389)"
          echo "✅ TurboVNC: ACTIVE (root/root on 5901)"
          echo "✅ Network Tools: INSTALLED"
          echo "✅ ALL SERVICES USE root/root"
          echo ""
          echo "⏳ Keeping alive for 6 hours..."
          tail -f /dev/null
