name: Ubuntu VPS - Full GNOME

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - uses: actions/checkout@v4

      - name: Install NetBird
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ca-certificates curl gnupg
          curl -sSL https://pkgs.netbird.io/debian/public.key | sudo gpg --dearmor --output /usr/share/keyrings/netbird-archive-keyring.gpg
          echo 'deb [signed-by=/usr/share/keyrings/netbird-archive-keyring.gpg] https://pkgs.netbird.io/debian stable main' | sudo tee /etc/apt/sources.list.d/netbird.list
          sudo apt-get update -y
          sudo apt-get install -y netbird

      - name: Connect NetBird
        env:
          NETBIRD_SETUP_KEY: ${{ secrets.NETBIRD_SETUP_KEY }}
        run: |
          sudo netbird up --setup-key $NETBIRD_SETUP_KEY --allow-server-ssh &
          sleep 5
          sudo netbird status

      - name: Install GNOME, RDP, VNC (No NetworkManager)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            openssh-server \
            xrdp \
            x11vnc \
            xvfb \
            gnome-session \
            gnome-shell \
            gnome-terminal \
            nautilus \
            gnome-tweaks \
            gnome-control-center \
            dbus-x11 \
            x11-utils \
            tigervnc-standalone-server \
            tigervnc-common \
            --no-install-recommends

          # Mask NetworkManager if it sneaks in
          if systemctl list-units --full -all | grep -q network-manager; then
            sudo systemctl stop network-manager 2>/dev/null || true
            sudo systemctl mask network-manager 2>/dev/null || true
          fi

      - name: Configure SSH (root/root)
        run: |
          echo "root:root" | sudo chpasswd
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh

      - name: Configure xRDP → Full GNOME
        run: |
          sudo useradd -m -s /bin/bash rdpuser || true
          echo "rdpuser:root" | sudo chpasswd
          sudo usermod -aG sudo rdpuser

          sudo tee /etc/xrdp/startwm.sh > /dev/null << 'EOF'
          #!/bin/sh
          . /etc/profile
          export HOME=/home/rdpuser
          export XDG_CURRENT_DESKTOP="GNOME"
          export XDG_MENU_PREFIX="gnome-"
          export GNOME_SHELL_SESSION_MODE="ubuntu"
          exec gnome-session
          EOF
          sudo chmod +x /etc/xrdp/startwm.sh
          sudo systemctl restart xrdp

      - name: Configure VNC → Full GNOME (port 5901)
        run: |
          sudo -u rdpuser mkdir -p /home/rdpuser/.vnc
          echo "root" | sudo -u rdpuser vncpasswd -f > /home/rdpuser/.vnc/passwd
          sudo chmod 600 /home/rdpuser/.vnc/passwd

          sudo -u rdpuser tee /home/rdpuser/.vnc/xstartup > /dev/null << 'EOF'
          #!/bin/sh
          export XDG_CURRENT_DESKTOP="GNOME"
          export XDG_MENU_PREFIX="gnome-"
          export GNOME_SHELL_SESSION_MODE="ubuntu"
          exec gnome-session
          EOF
          sudo -u rdpuser chmod +x /home/rdpuser/.vnc/xstartup

          # Start VNC via Xvfb + x11vnc (runs as rdpuser)
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3
          sudo -u rdpuser DISPLAY=:99 gnome-session &
          sleep 6
          sudo -u rdpuser x11vnc -display :99 -rfbport 5901 -forever -nopw -passwd root -shared -listen 0.0.0.0 &

          # Disable PolicyKit auth checks (optional)
          sudo sed -i 's/^#*Auth      :.*$/Auth      : no/' /etc/dbus-1/system.d/org.freedesktop.PolicyKit1.conf 2>/dev/null || true
          sudo systemctl restart dbus 2>/dev/null || true

      - name: Connection Info
        run: |
          IP=$(sudo netbird status | grep "NetBird IP" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          echo "=========================================="
          echo "✅ UBUNTU VPS READY - FULL GNOME"
          echo "=========================================="
          echo ""
          echo "📡 NetBird IP: $IP"
          echo ""
          echo "🔑 SSH (port 22):"
          echo "  ssh root@$IP"
          echo "  Password: root"
          echo ""
          echo "🖥️ xRDP (port 3389):"
          echo "  Address: $IP:3389"
          echo "  User: rdpuser"
          echo "  Password: root"
          echo "  → Desktop: Full GNOME"
          echo ""
          echo "🖥️ VNC (port 5901):"
          echo "  vncviewer $IP:5901"
          echo "  Password: root"
          echo "  → Desktop: Full GNOME"
          echo "=========================================="
          echo ""
          echo "⏱️ Server runs for 6 HOURS"
          echo "📌 Auto-starts on commit to main/master"
          echo "✅ NetworkManager is DISABLED - NetBird stays connected"
          echo "✅ You can do other work now!"

      - name: Keep Alive (6 hours)
        run: |
          echo "=========================================="
          echo "🚀 SERVER IS RUNNING - FULL GNOME"
          echo "=========================================="
          echo ""
          echo "✅ NetBird connected"
          echo "✅ SSH running on port 22"
          echo "✅ xRDP running on port 3389 (GNOME)"
          echo "✅ VNC running on port 5901 (GNOME)"
          echo "✅ NetworkManager is DISABLED"
          echo ""
          echo "⏳ Keeping alive for 6 hours..."
          echo "   GitHub will auto-stop after 6 hours"
          echo ""
          tail -f /dev/null
